apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.streamingService.name }}
  labels:
    app: {{ .Values.streamingService.name }}
spec:
  replicas: {{ .Values.streamingService.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.streamingService.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.streamingService.name }}
    spec:
      containers:
      - name: {{ .Values.streamingService.name }}
        image: "{{ .Values.image.registry }}/{{ .Values.image.prefix }}/streaming:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - containerPort: {{ .Values.streamingService.port }}
        env:
        - name: NODE_ENV
          value: {{ .Values.env.NODE_ENV }}
        - name: PORT
          value: "{{ .Values.streamingService.port }}"
        resources:
          {{- toYaml .Values.streamingService.resources | nindent 10 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.streamingService.name }}
spec:
  selector:
    app: {{ .Values.streamingService.name }}
  ports:
  - protocol: TCP
    port: {{ .Values.streamingService.port }}
    targetPort: {{ .Values.streamingService.port }}
  type: ClusterIP
